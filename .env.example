# This project runs its LLM locally via Ollama and needs NO secret keys.
# This file is a placeholder so the (gitignored) .env pattern is documented.
# If you later swap in a hosted LLM, add its key here, e.g.:
# OPENAI_API_KEY=your-key-here
